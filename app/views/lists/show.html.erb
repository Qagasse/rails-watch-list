<h1>Lists#show</h1>
<p><%= @list.name %></p>

<h2>Bookmarks</h2>

<ul>
    <% @list.bookmarks.each do |bookmark| %>
        <li><%= bookmark.movie.title %>: <%= bookmark.comment  %> <%= link_to "Destroy", bookmark_path(bookmark), method: :delete, data: {confirm: "Are you sure"} %></li>
    <% end %>
</ul>

<%= link_to "Back to list", lists_path  %>
<%= link_to "Add Bookmark", new_list_bookmark_path(@list)  %>



